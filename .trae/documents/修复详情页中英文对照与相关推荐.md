# 修复详情页中英文对照与相关推荐

## 1. 修复中英文对照显示 (`PromptDetailPage.tsx`)
- **问题**：目前详情页仅显示 `content` (英文)，忽略了 `chineseContent`。
- **修复**：
  - 在英文代码块下方，添加一个可折叠或直接显示的中文翻译区域。
  - 使用 `prompt.chineseContent` 字段。
  - 样式参考现有的代码块风格。

## 2. 修复相关推荐加载逻辑 (`PromptDetailPage.tsx`)
- **问题**：目前依赖首页随机列表的第一页数据，导致命中率极低。
- **修复**：
  - 移除对 `usePrompts()` 的依赖。
  - 添加一个新的 `useEffect`，当 `prompt.category` 确定后，直接调用 API `/api/prompts?category=XXX&limit=4&sort=random` 获取同类推荐。
  - 将获取到的数据存入本地 state `relatedPrompts`。

## 3. 执行顺序
- 修改 `PromptDetailPage.tsx` 实现上述两点。
